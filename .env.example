# By default, localhost uses local and real domains use production.
# Uncomment to force one backend:
# VITE_API_BACKEND=local

# production calls: https://api.trackpal.app/api/...
# VITE_API_BACKEND=production
